{
  "name": "charts-agent-container",
  "version": "1.0.0",
  "description": "Charts Agent Container Service - Technical Analysis with Python ML",
  "main": "index.ts",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "echo 'No build step required for Bun runtime'"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
